const DocumentsPage = () => {
  return (
    <section>
      <h1 className="text-2xl font-semibold mb-2">Documents</h1>
      <p className="text-slate-400 text-sm">
        Upload PDFs, text files and other documents to make them searchable and
        ready for AI.
      </p>
    </section>
  );
};

export default DocumentsPage;
